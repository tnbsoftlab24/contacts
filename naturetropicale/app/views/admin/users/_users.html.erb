<ul class="nav nav-pills mt-4 mb-3" id="pills-tab" role="tablist">
  <li class="nav-item" role="presentation">
    <a class="nav-link active" id="pills-home-tab" data-toggle="pill" href="#pills-home" role="tab" aria-controls="pills-home" aria-selected="true">Comptes en Attente de validation (<%= @users_unconfirmed.count %>)</a>
  </li>
  <li class="nav-item" role="presentation">
    <a class="nav-link" id="pills-profile-tab" data-toggle="pill" href="#pills-profile" role="tab" aria-controls="pills-profile" aria-selected="false">Comptes Validés (<%= @users_confirmed.count %>)</a>
  </li>
</ul>
<div class="tab-content" id="pills-tabContent">
  <div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab">
    <%= render partial:'admin/users/users_unconfirmed'%>
    <%= will_paginate @users_unconfirmed, param_name: 'unconfirmed_page', :params => { :anchor => 'pills-home' }, list_classes: %w(pagination mt-4 justify-content-center) %>
  </div>
  <div class="tab-pane fade" id="pills-profile" role="tabpanel" aria-labelledby="pills-profile-tab">
    <%= render partial:'admin/users/users_confirmed'%>
    <%= will_paginate @users_confirmed, param_name: 'confirmed_page', :params => { :anchor => 'pills-profile' }, list_classes: %w(pagination mt-4 justify-content-center) %>
  </div>
</div>


<script>
  // open tab on click
  $(function(){
    var hash = window.location.hash;
    hash && $('ul.nav a[href="' + hash + '"]').tab('show');

    $('.nav-tabs a').click(function (e) {
    $(this).tab('show');
    var scrollmem = $('body').scrollTop();
    window.location.hash = this.hash;
    $('html,body').scrollTop(scrollmem);
    });
  });
</script>
