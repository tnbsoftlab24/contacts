<table id="users" class="table-striped table-responsive-sm table nowrap" style="width:100%">
  <thead>
      <tr>
        <th >Email</th>
        <th >Date de création</th>
        <th colspan="2">Status</th>
        <th  >Actions</th>
      </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td scope="row">
          <%= user.email %>
        </td>
        <td><%= user.created_at %></td>
        <td colspan="2" ><%= user.created_at %></td>
        <td><%= link_to "Verifier Cv ", user.profile.cv %></td>
        <td class="d-flex align-items-center ">
          <% if user.profile.present? %>
            <label class="badge badge-danger">compte confirmer</label>
          <% else  %>
            <label class="badge badge-success">compte suprimer</label>
          <%end%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @users, list_classes: %w(pagination justify-content-center) %>



