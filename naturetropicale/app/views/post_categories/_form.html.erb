<div class="container">
  <div class="row form-height">
    <div class="col-10 col-sm-8 col-md-7 col-lg-7 col-xl-6 mx-auto form-box">
      <%= form_with(model: post_category, local: true) do |form| %>
        <% if post_category.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(post_category.errors.count, "error") %> prohibited this post_category from being saved:</h2>
            <ul>
              <% post_category.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
                
        <div class="field">
          <%= form.label :title %>
          <%= form.text_field :title, {class: "form-control"} %>
        </div>

        <div class="field">
          <%= form.label :parent_id %>
          <%= form.collection_select :parent_id, PostCategory.all, :id, :title, {include_blank: "Choisir une catégorie..."}, {class: "form-control"} %>
        </div>

        <div class="actions mt-5">
          <%= form.submit class:"btn btn-block btn-lg " %>
        </div>
      <% end %>
    </div>
  </div>
</div>
